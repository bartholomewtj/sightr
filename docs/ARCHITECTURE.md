# Architecture

This file exists so older ADR and `HERDR_API.md` links still resolve.

The live map of how Sightr is built is [README.md → Architecture](../README.md#architecture), plus the interactive maps under [`archify/`](archify/). Security and Tailscale facts (loopback bind, Host/Origin allowlists, identity header, reconnect lock) also live in the README.
